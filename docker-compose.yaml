version: '3'

services:
  discord_geminiai:
    build: .
    environment:
      - DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - BARD_SECURE_1PSID=${BARD_SECURE_1PSID}
      - BARD_SECURE_1PSIDTS=${BARD_SECURE_1PSIDTS}
      - GEMINI_SETTING_CHANNEL_ID=${GEMINI_SETTING_CHANNEL_ID}
      - GEMINI_CHAT_CHANNEL_ID=${GEMINI_CHAT_CHANNEL_ID}
      - BARD_CHAT_CHANNEL_ID=${BARD_CHAT_CHANNEL_ID}
      - BARD_COOKIES_SETTING_CHANNEL_ID=${BARD_COOKIES_SETTING_CHANNEL_ID}
      - HELP_CMD_CHANNEL_ID=${HELP_CMD_CHANNEL_ID}